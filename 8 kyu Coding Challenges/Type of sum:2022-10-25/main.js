// Return the type of the sum of the two arguments

// typeOfSum(12, 1), 'number';
// typeOfSum('d', 1), 'string';
// typeOfSum(1, 'a'), 'string';

function typeOfSum(a, b) {
    // add together
    let sumOfTwo= a + b
    // find typeoOf sum
    return (typeof sumOfTwo)
    }